// Mirrors src/lib/constants.js's ROLES_LIST on the frontend — kept as a
// small standalone constant here rather than a shared package, since these
// are two separately-deployed projects.
export const ROLES_LIST = ['Master Admin', 'Creator', 'Reviewer', 'Approver', 'Approver 2', 'Member', 'Trainer', 'Trainee'];
